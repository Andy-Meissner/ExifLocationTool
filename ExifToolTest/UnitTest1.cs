using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using XperiCode.JpegMetadata;

namespace ExifToolTest
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {/*
            CustomImage ci = new CustomImage(@"C:\Users\andy\Desktop\pics\4.jpg");
            Exif exif = new Exif(ci);
            for (short i = 0; i < 10; i++)
            {
                exif.SetCountryName("üänä", i);
                exif.saveImage(i);
            }
            ci.Image.Dispose();*/
        }
    }
}
